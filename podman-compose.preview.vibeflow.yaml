version: '3.8'
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.preview
    network_mode: host
    # Next.js will use port 3000 and bind to 0.0.0.0 as per Dockerfile CMD
    # The VibeFlow proxy will handle routing to this port.
